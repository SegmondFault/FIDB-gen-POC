SITE_URL=http://localhost:3000

# Server-only loopback coordinator. Browsers use /api/fidb/* on the control
# panel origin, including when the panel is opened over Tailscale.
FIDB_API_ORIGIN=http://127.0.0.1:8765
