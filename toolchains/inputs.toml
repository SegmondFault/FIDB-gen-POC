schema_version = "fidb-toolchain-inputs/v1"

# No private payload is currently accepted into the Linux project store.
# Apple SDKs remain installed and used only on an external Apple worker.
input = []
